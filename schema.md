# Schema, Find My Mahj Game (retired)

This file is retired. It described an aspirational data model (Connectors / Connections / Referrals, Supabase Auth) that never matched production and should not be built against.

The current and target schema, the full membership and listing system, user roles, tiers, the teacher profile system, the event model, the ambassador program, rankings, the monetization roadmap, and the additive (no-rebuild) migration plan now live in:

`docs/membership-system-spec.md`

- Section H: exact tables, enums, foreign keys, indexes, RLS policies, and the ordered migration checklist.
- Section I: how the design maps onto the live tables, plus the cleanup checklist.
- Section J: phased build order.
