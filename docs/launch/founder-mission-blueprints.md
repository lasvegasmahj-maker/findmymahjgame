# FMG Founder Mission Blueprints (build-ready)

Retention, First-1000, Referral, Teacher Love, Host Success, Success Path, Failure Dashboard, Love Map.

## Retention Blueprint: make the recurring local game the habit, and make Find My Mahj Game the lightweight tool attached t
_A player returns because they have a standing game we help them keep alive; build the recurring-table loop first (it is currently a dead column), then the sub-fill Bench and the weekly digest that surround it._

1. **Make tables actually recurring: close the loop from a played game to a standing weekly game** (S (one page branch, one API route, no schema change; is_recurring already exists), Unlocks the only retention mechanic the corpus calls a moat,)
   - Build: After a host taps 'Yes, we played' on the ask-played email, send them to a one-tap 'Make this our standing game' page that sets is_recurring=true and confirms day_of_week + time_of_day. Today is_recurring is a real column (sprint2.sql) read on /admin/metrics but NEVER set to true anywhere in the cod
2. **Standing-game weekly reminder for recurring tables** (M (one cron route + schedule entry + email template; depends on rank 1 setting is_recurring), Converts a passive recurring flag into an active weekly habi)
   - Build: Add a daily cron (extend the existing Vercel cron pattern in vercel.json alongside ask-played and matcher) that, for each table where is_recurring=true, emails the host (and optionally all seated players) a short reminder the day before their standing day_of_week: 'Your Tuesday Summerlin game is tom
3. **Need a Sub: the Bench, wired to existing recurring tables (not just new-table matching)** (M (new action route + email broadcast, reuses match-decide approval, dark by default), Protects recurring tables from the #1 cause of death (a drop)
   - Build: The matcher (app/api/cron/matcher/route.ts) and the Bench foundation (match_drafts, play_requests.status, app_settings.matcher_enabled) are built but ship DARK and only pool brand-new play_requests into NEW tables. Add the missing piece the corpus calls 'the most viral, highest-intent event in the w
4. **Automated weekly digest email (the habit for the weeks between games and for players not yet in a game)** (M (weekly cron querying tables, building the city-segmented need-a-4th block, sending via existing email choke point), Gives players not yet in a recurring game a weekly, actionab)
   - Build: For 90 days the newsletter stays HAND-WRITTEN per the corpus (doc 29, doc 33: 'no newsletter software, no automation build... prove the format first'), so do NOT build full newsletter automation now. Instead build the one automated piece that is pure action and changes weekly: a 'Tables near you nee
5. **Freshness loop: confirm-still-active pings for listings, hosts, and teachers** (S-M (one monthly cron + confirm route, reuses claims magic-link infra), Keeps the freshness loop honest so the weekly digest and dis)
   - Build: The freshness columns exist (confirmed_active_at, ended_reports on venue_listings and event_listings, added in 2026-06-12-claims-freshness.sql) and ordering already favors confirmed rows, but I found NO cron that actually asks hosts/venues/teachers to confirm. Add a low-frequency cron (monthly) that

   Build now: Recurring-table loop (rank 1): post-'Yes we played', one-tap 'Make this our standing game'; Standing-game weekly reminder cron (rank 2): day-before reminder for is_recurring tables w; Need a Sub / Bench wiring (rank 3): connect a losing seat on a recurring table to the loca; Automated weekly need-a-4th digest (rank 4): the one auto-generated, weekly-changing, acti; Freshness confirm-ping cron (rank 5): monthly one-tap 'still happening?' to listing/venue/
   Watch: Recurring rate: of tables that reached Played, the percent t; Standing-game survival: of is_recurring tables, the percent ; Need-a-sub fill rate: of sub requests broadcast to the Bench; Weekly digest ACTIONS not opens: seats claimed and tables st; Return-by-action: distinct players who took a meaningful act

## First 1,000 Players Roadmap, Texas-first. Be the obvious default for finding American Mahjong PEOPLE: a player matcher p
_Pick ONE Texas metro (DFW), ship the Bench plus the standing-table loop, seed it with the already-verified Dallas inventory and 195-person list, and run a hand-made weekly newsletter plus a founder daily matcher (/admin/today) until real games are happening, then clone the playbook to San Antonio and Houston._

1. **Ship the ONE Bench (city seat pool + daily approve-gated matcher) for DFW** (M (1-2 weeks; mostly wiring existing tables + the host-approval gate), Turns inbound demand into the first 30-50 seated players and)
   - Build: Build BUILD #1 from RULINGS.md: merge Fourth Chair + Sub Bench + Re-homing + Snowbird + Mover into one DFW seat pool fed by /play (play_requests already live). Add the 'this week vs ongoing' field, the host-approval gate before any stranger joins (non-negotiable safety gate from RULINGS), and the 3-
2. **Stand the standing-table loop: 5-6 person rosters + Run It Back + recurring** (M (the played-link and stamps exist; needs tables.root_id, clone-with-roster, is_recurring), Moves 10-15 first games into 100 recurring tables; recurring)
   - Build: Implement the table rulings: tables default to a 5-6 person roster (the table that lives at four dies at four), and Run It Back = clone-with-roster + is_recurring on the played=yes page (the minimal link is already in code). Ship the second-game default as an A/B (explicit button vs opt-out) on the 
3. **Launch the weekly DFW newsletter, hand-made, from the 195 real subscribers** (S (signup UI + /api/subscribe already built; needs keys, import, and a weekly hour to write), Drives the 500-subscriber goal and is the weekly engine that)
   - Build: Wire Mailchimp keys into Vercel (key + Audience ID, never in chat), import the 195 deduped subscribers (FMG-newsletter-import-CLEAN.csv), send Issue 1 this week. Same skeleton every issue, senior-friendly, one screen: (1) Tables needing players this week, one-tap claim link; (2) New tables forming n
4. **Publish and claim the verified DFW inventory; run teacher outreach as the supply engine** (S-M (inventory is verified and staged; this is approval + warm outreach, phone-and-Airtable work), Builds toward 100 teacher relationships and gives DFW real c)
   - Build: Bulk-approve the staged DFW pilot inventory in /admin after deploy (13 teachers, 12 events, 7 venues, all verified; hold any NV teacher, any schedule-less open play, any row missing provenance). Send the Graduation Kit + claim invite to each listed teacher/host (claim flow at /claim is built) so lis
5. **Recruit and activate 3-5 DFW host/ambassadors and hand them tables** (M (relationship work; onboarding scripts and referral codes exist, one small SQL delta for ambassadors.referral_code outstanding), Each activated host carries several recurring tables, compou)
   - Build: From the existing warm contacts + Potential-Ambassadors CRM, recruit a small number of DFW hosts. Activation, not signatures: an ambassador counts only after they host a table, list a venue, bring a teacher, or recruit 3+ players. Use the recruiting + onboarding sequences already drafted; do the fir
6. **Run the Friday Source-of-Truth scorecard and the Dallas Test tripwire** (S (the scorecard, /admin/metrics, and /admin/today are built), Keeps the whole plan honest and forces the one weekly decisi)
   - Build: Every Friday, 15 minutes, count real things only and undercount when unsure (the scorecard is already written). Headline = Games Played: 'how many times did mahjong actually get played because of us this week?' Watch the >=50% confirmation guardrail welded to the North Star. Hold the Day-60 tripwire

   Build now: Ship the ONE Bench for DFW: city seat pool from /play (play_requests) + the host-approval ; Stand up the standing-table loop: 5-6 person rosters (no four-seat tables) + Run It Back c; Launch the weekly DFW newsletter this week: put Mailchimp keys in Vercel, import the 195 r; Approve and claim the verified DFW pilot inventory (13 teachers, 12 events, 7 venues) and ; Recruit and ACTIVATE 3-5 DFW hosts (activation = hosted/listed/brought a teacher/3+ recrui
   Watch: Weekly Player-Games Confirmed (North Star) with the >=50% co; Recurring tables in DFW that have played 3+ times (the moat ; Conversion event count: third confirmed played game with the; Bench throughput: /play requests in -> matches approved in /; Newsletter: subscriber count toward 500, and engagement meas

## Referral Engine: the moments that make a 45-70 woman say "you need to check out Find My Mahj Game," and the share mechan
_The peak word-of-mouth moments are joy ("our table is full") and relief ("I found my 4th this week"), not signup. The product already nails the urgency-share moment (1 seat left) but throws away the two biggest evangelist moments (table-fills email and "Yes, we played"). Ship a share/refer mechanic into each of those, give every host a personal shareable link, and a 45-70 woman will do the marketing for you._

1. **Add a share + invite mechanic to the 'Your table is full' email (the peak-joy moment)** (S (edit one existing email template + generate/store a host ref code), Every filled table becomes 4 warm broadcasters at their happ)
   - Build: In app/api/tables/claim/route.ts, the full-table BCC-to-all email currently ends at 'pick a spot' with no share ask. Add a warm block: 'Loved getting a table together? Help a friend find one too,' followed by the host's personal link findmymahjgame.com/start?ref=HOSTCODE and a single 'Forward this t
2. **Turn the 'Yes, we played' confirmation into the evangelist + testimonial moment** (S (one page + a moderated quotes table; ShareSheet already exists), Converts proven-happy players into sharers and supplies hone)
   - Build: app/played/page.tsx result=yes currently only shows 'Run it back: same time next week.' Add, beside it: (a) a one-tap ShareSheet with copy 'We found our mahjong group on Find My Mahj Game and just played our first game,' and (b) a 1-question optional testimonial capture ('What did you love? We may f
3. **Give every host a personal share link with self-serve attribution (not just ambassadors)** (M (code generation reuse + metrics rollup extension; doc 09 Phase 2 already scopes this), Reveals which players (not just ambassadors) drive new table)
   - Build: Today ?ref= and referred_by are ambassador-only (lib captures FMM-CITY-NAME). Generate a personal code for every host on table create (reuse the share_code already minted in api/tables/create), expose it as findmymahjgame.com/start?ref=PERSONALCODE on the 'Your table is up!' screen (app/t/[code]/pag
4. **Wire and rewrite the orphaned share-preview into a real post-listing 'You're in, invite your table' page** (M (connect + rewrite copy + add group-invite link; page styling already done), Turns the dead-end success screen into a viral loop right at)
   - Build: app/share-preview/page.tsx is live but uses MOCK_PLAYER, contains an emoji (violates the no-emoji hard rule), and has an X/Twitter button (wrong channel for this audience). Rewrite to real data, swap the SHARE_TEXT to the group-oriented first-person copy from doc 04, drop X, lean on the existing Sha
5. **Build the 'Get notified when the new card drops' email capture now, fire the spring referral blast later** (S for capture module; campaign later, Within 90 days this is list-building, not payoff, but it is )
   - Build: Add a small 'New NMJL card releases every spring, get the alert and find a group to learn the new hands together' email-capture module to the homepage and state pages (no month named, per hard rules), writing to the newsletter list. When the card releases, blast: 'Learning the new card is more fun t

   Build now: Item 1: Add the share/invite block to the 'Your table is full' email (peak-joy moment) in ; Item 2: Rebuild the 'Yes, we played' screen (app/played/page.tsx) with a one-tap ShareShee; Item 3: Generate a personal share link + attribution for every host (not just ambassadors); Item 4: Wire and rewrite the orphaned app/share-preview page into a real post-listing 'inv
   Watch: Share-button tap rate: % of hosts who reach the 'table is up; K-factor: (invites/shares sent per host) x (invite-to-new-ta; Referred-table share: tables with a non-null referred_by as ; Need-a-4th fill rate: % of tables that reach 1-seat-left and; Played-confirmation rate, and of those, share-tap rate and t

## Teacher Love Index: rank every shippable feature by how much it makes a teacher actively promote Find My Mahj Game (ment
_A teacher promotes FMG when it makes her look good to her students and solves their post-class "now what" with zero work for her; so ship the student-handoff link, the claim-plus-graduation-kit, and priority referrals first, and never make her pay or sell._

1. **Teacher student-handoff link (per-teacher tracked /play link + 1-line class script)** (M (reuse ref-code plumbing, build a /play?city= prefill + a teacher one-pager generator), Every active teacher becomes a player-supply pipe; turns one)
   - Build: Ship a per-teacher referral link (reuse the ambassadors.referral_code mechanic, the one SQL delta already staged) that deep-links to /play prefilled to her city, plus the canned one-liner she forwards to every graduating class: 'When you are ready for a regular game, here is the easiest place to fin
2. **Claim-your-listing + 14-day Graduation Kit (the claim flow already exists; add the kit as the post-claim payoff)** (S-M (claim flow live; build the on-claim kit email + assemble her personalized assets), Converts passive listings into owned, promoted listings; eac)
   - Build: Send every real teacher a claim magic link (HMAC, /claim already built) so she owns and corrects her own free listing in minutes. The instant she claims, trigger the Graduation Kit email: her handoff link + QR, the 3 social/flyer scripts from the launch kit, and 'here is how to send a class.' RULING
3. **Priority referrals: 'when a player near you asks for a teacher, you are who we send'** (M (add lesson-seeker capture; build the founder-routed referral queue, earned-gate aware), Strongest retention/advocacy lever; the reason a teacher put)
   - Build: Wire the lessons-intent path (the /teachers near-search and a 'looking for a teacher' capture) to route new local lesson-seekers to claimed, active teachers first, ranked by freshness (confirmed_active_at) not by payment. Tell her plainly: when someone in your neighborhoods wants lessons, you get fi
4. **Founding Teacher recognition: numbered badge on her listing + named newsletter shout-out** (S (a badge field + render on listing; newsletter is manual), Cheap, high-warmth advocacy; turns a listed teacher into a p)
   - Build: At launch, ship ONE honor: a 'Founding Teacher' badge that renders on her claimed listing, awarded by the founder (not self-serve), plus a warm named thank-you in the newsletter when she hits the Active gate. Mirror the Founding Ambassador numbered-email pattern already ruled in. No leaderboards, no
5. **Teacher-to-Ambassador crossover invite (the upgrade path, post-first-shared-link)** (S (copy exists; trigger off first-share + set a People tag), Moves the best teachers up the love curve into city-captain )
   - Build: After a teacher claims and shares one student link, send the prepared crossover email inviting her to be a Founding Ambassador (badge on listing, priority referrals, a say in how FMG grows her city). The crossover copy is already written in the acquisition blitz doc; wire it to fire off the 'shared 
6. **Teacher profile + featured placement (city-first, real-data-gated to Phase 2)** (M (profile page + featured ordering; gated on real teacher count), Compounds advocacy once density exists; not a 90-day mover u)
   - Build: Build the individual teacher profile page (first name, neighborhoods, what she teaches, beginner-friendly flag, her own website link out) and earned 'featured first in her area' placement. Hold the public directory/profiles until ~20 real published teachers exist (the launch bar already wired into a

   Build now: Teacher student-handoff link: per-teacher tracked /play link (reuse referral_code, paste t; Claim + auto Graduation Kit: send every vetted teacher her HMAC claim link (flow already b; Priority referrals routing: add a 'looking for a teacher' capture and route local lesson-s; Founding Teacher badge + named newsletter shout-out: one launch honor that renders on her ; Teacher-to-Ambassador crossover trigger: after she claims and shares one link, auto-send t
   Watch: Teachers claimed / total teachers listed (claim rate): the s; Graduation Kits delivered within 14 days of claim (target 10; Students arriving via a teacher handoff link (player listing; Teachers who reach the Active gate (3 students found games O; Teacher-sourced Weekly Player-Games Confirmed (share of the 

## Host Success Blueprint: make the host (the table starter/coordinator) the most protected, least-burdened person in the p
_Hosts trust FMG when it removes the three things they fear (strangers, the no-show that kills the game, and the work), so build the Bench, the standing-table loop, and a done-with-you host concierge before anything else._

1. **The Bench: one approve-gated city seat pool that backfills any open seat** (High (new matcher, approval flow, seat-pool schema, host comms). Already RULINGS BUILD #1, so it is sanctioned and gated; build the approval gate first., Directly protects every recurring table from collapse; conve)
   - Build: Ship the ONE Bench per RULINGS #1: merge Fourth Chair, Sub Bench, Re-homing, Snowbird, Mover into a single city seat pool with one daily host-approve-gated matcher and a 'this week vs ongoing' field. Host taps 'I need a 4th this week' from her table page or the played email; the matcher proposes 1-3
2. **Standing-table loop: Run It Back + 5-6 roster default + the played confirmation as the re-engagement hook** (Medium. The played email, token flow, and table schema exist; this adds is_recurring, a roster size, and a clone action plus the A/B., Moves games from Played (one-time) to Recurring (the moat). )
   - Build: Complete the standing-table loop on top of the existing played-confirmation email (app/api/tables/played + lib/email.ts already exist). After a 'yes, we played' confirmation, offer clone-with-roster + is_recurring so the host books next week in one tap (RULINGS: Run It Back clone-with-roster + is_re
3. **Host Concierge: done-with-you setup so a hesitant host never carries the work alone** (Medium. /admin/today and the invite kit are real builds; the concierge itself is an operations motion using existing email + admin tools., Raises activation (created -> filled -> played) and is the l)
   - Build: Build a host concierge motion + the assets that make it scale: (a) a 'we will do the setup with you' path on /start and in the host onboarding email (the warm-activation script already promises 'I will do the setup with you'); (b) an /admin/today host-attention view so the founder/ambassador sees ev
4. **Host trust + control surface: never expose her, always show she is in charge** (Low-Medium. Much is IN CODE; this is mostly copy, control tags, and wiring the four trust lines. No reviews/stars/points ever (RULINGS DEAD by design)., Removes the #1 adoption blocker (fear of strangers/exposure))
   - Build: Harden and surface the host trust guarantees already half-built: keep no attendee names and no home address on the public table page (IN CODE, hold the line); make 'You approve every player before they join' and 'We never show your phone or address' explicit on /start and the table page; give the ho
5. **Graduation Kit + Founding Host recognition within 14 days of every claim** (Low-Medium. Recognition email is copy + the existing ref-code issuance; claims flow is a real but bounded build., Improves host retention and word-of-mouth supply (listings b)
   - Build: Ship the claim flow and the within-14-days Graduation Kit as a host motion (RULINGS: graduation kit within 14 days of every claim is the highest-ROI founder action). For hosts who claim/run a standing table, send the single launch honor allowed: a numbered Founding Ambassador email in Shauna's voice

   Build now: The Bench (ONE approve-gated city seat pool, host approval gate first, Dallas pilot, 3 tab; Standing-table loop: Run It Back clone-with-roster + is_recurring on top of the existing p; Host Concierge + /admin/today: a done-with-you setup motion and a daily host-attention vie; Host trust + control surface: explicit 'you approve every player, we never show your phone; Graduation Kit + Founding Host recognition within 14 days of every claim: one numbered hon
   Watch: Weekly Player-Games Confirmed (North Star) with the >=50% co; Funnel conversion at each host stage: Tables Created -> Fill; Recurring Tables (table playing twice+ on a standing day/tim; Bench fill rate: share of 'need a 4th this week' requests th; Run It Back rate: share of Played games that rebook a next g

## Work backwards from FMG dominant in 5 years (the default place American Mahjong players go to find their people). The de
_FMG wins by owning the recurring-table habit in real metros (DFW first, proven without the founder), wired to a weekly newsletter and an approve-gated Bench that fills seats, never the breadth of a directory. Ship the standing-table loop, the Bench, and the weekly newsletter; measure only games actually played._

1. **Standing-table loop: root_id lineage + Run-It-Back clone-with-roster + is_recurring (the moat, currently spec-only)** (Small-medium: one additive migration plus one page + clone action; the email/token plumbing already exists., Converts one-time fills into standing games; lifts the headl)
   - Build: Apply the table-lineage migration (tables.root_id, is_recurring) and ship a one-tap Run It Back on the played=yes page that clones the roster into next week's session, same day/time/venue. Default to a single A/B on the first 12-20 tables (explicit button vs opt-out, per RULINGS.md) and stamp every 
2. **The Bench: one approve-gated city seat pool that fills empty seats (matcher built but ships DARK and Dallas-only)** (Medium: host-approval gate + seed the pool + flip the flag; ~70% is already in code., Turns dropped seats and scattered players into filled tables)
   - Build: Keep the matcher dark behind app_settings.matcher_enabled=false until the host-approval safety gate is in code (RULINGS: no stranger pooling without host approval). Build that one missing gate, seed real DFW play_requests from the Dallas pilot inventory, then flip the matcher on with the 3-tables/da
3. **Weekly local newsletter, sent every week without fail (the habit engine and owned distribution)** (Small to start (keys + import + send), then a recurring weekly operating habit, not a build., Drives returning visitors and seeds every table fill and eve)
   - Build: Wire MAILCHIMP_API_KEY + Audience ID into Vercel (never chat), import the 195 deduped subscribers, send Issue 1 the week of launch, then a fixed-day issue every week. Same skeleton each issue: this week's games/open plays, one featured teacher/venue, upcoming events, a Looking for a game? push to /s
4. **/admin/today as the single founder cockpit + the North Star hero tile (Weekly Player-Games Confirmed with the +50% confi** (Small: compute one metric from existing columns + a tile + in-page approve buttons., Keeps the quarter pointed at real games, surfaces stalled ta)
   - Build: Finish /admin/today as the one screen the founder lives in: it already rolls up pending listings, inquiries, ambassador apps, claimer edits, and match drafts (the only safety net for drafts). Add the North Star hero tile (Weekly Player-Games Confirmed) computed off the played_at/filled_at stamps alr
5. **Teacher claim + graduation kit (relationships now, public directory later) as the supply land grab** (Small build (claim invites use existing routes) + an ongoing weekly outreach motion., Seeds verified inventory and a player pipeline in DFW and be)
   - Build: Use the claims/freshness build already migrated (listing_claims, pending_edits, magic-link claim, confirmed_active_at) to send claim invites to the warm Oh My Mahjong and Dallas teachers, one teacher-to-teacher note from the founder. Within 14 days of every claim, send the graduation kit (the corpus

   Build now: Standing-table loop: ship tables.root_id + is_recurring migration and a one-tap Run It Bac; Flip the Bench live in DFW: build the one missing host-approval gate, seed real Dallas pla; Newsletter weekly habit: put Mailchimp keys in Vercel, import the 195 subscribers, send Is; /admin/today cockpit + North Star hero tile: finish the founder's one screen with in-page ; Teacher claim + 14-day graduation kit: use the already-migrated claims/freshness plumbing 
   Watch: Weekly Player-Games Confirmed (THE North Star): distinct pla; Confirmation rate guardrail (>=50%): of tables that filled, ; Recurring tables active this week: standing games that have ; DFW liquidity for the Dallas Test (review Sept 9): tables cr; Non-founder hosts: count of recurring tables handed to a non

## Failure Dashboard: assume Find My Mahj Game dies, name what killed it, the warning sign that showed first, and instrumen
_FMG dies as an empty room or a museum of dead links, and the first warning sign is always the same: vanity numbers (signups, listings, traffic) climbing while the one honest number, games actually played by real people, stays flat, so instrument the funnel-leak ratios now and treat any gap between coverage and games-played as the alarm._

1. **DEATH BY COLD-START: the empty room. A real woman searches her city this week and finds nobody to play with, so the firs** (Medium, Highest. If DFW (the named liquidity proof market) cannot ha)
   - Build: Build the Liquidity-Per-Active-City alert into /admin/today: for every city with any traffic or a listed table, show seekers-this-week vs fillable-seats-this-week. ALERT (red) when a city has 3+ seekers in 14 days and zero filled tables, or any table sits at 1-of-roster for 21+ days. Pull from playe
2. **DEATH BY NO GAMES PLAYED: the directory nobody uses. Coverage (events, teachers, venues, signups) races up while real ga** (Low, Highest. This is the existential honesty check and it is the)
   - Build: Instrument the Headline Tripwire: Weekly Player-Games Confirmed must be > 0 and rising, with the >=50% confirmation-rate guardrail welded beside it (played confirmations / filled tables). ALERT when any coverage metric grows two weeks in a row while Games Played is flat or zero. Surface the Coverage
3. **DEATH BY STALE INVENTORY: the source-of-truth claim collapses. Listings go dead, an event has already happened, a teache** (Medium, High. Trust is nearly impossible to rebuild with this demogr)
   - Build: Build the Freshness Monitor: every listing carries a last-confirmed date and a status (Verified vs Public listing). ALERT when any published event is past its date and still live, when any listing has not been re-confirmed in 90 days, or when the share of Verified listings drops below a floor. Auto-
4. **DEATH BY RETENTION LEAK: the foursome forms and leaves. Four women click into a standing Tuesday game, swap numbers, sta** (Medium, High. Without return, every dollar of acquisition leaks; the)
   - Build: Instrument the Return-After-Forming metric: of players who formed or filled a table, what share take a second action (find a 4th, run-it-back, open the newsletter, claim a sub) within 28 days. Track PTWM (players with 2+ confirmed sessions, same table lineage, in 28 days) as defined in RULINGS.md. A
5. **DEATH BY FOUNDER-ONLY PROOF: it works because Shauna does it. The only tables that form are founder-pushed; no one else ** (Low, High. Determines whether market 2 is launchable or whether e)
   - Build: Build the Organic-Ratio gauge: founder-assisted tables vs ambassador-or-self-serve tables, per city. Tie to the Day-30 proof bar already written in founder-assumptions (at least one recurring game an ambassador other than the founder created and runs, played 2+ times). ALERT at the Day-60 tripwire (
6. **DEATH BY SAFETY OR TRUST BREACH: one bad incident or one leaked phone number ends it. With a senior audience, a single p** (Medium, Catastrophic if it fires, low-probability if guarded. The as)
   - Build: Instrument continuous safety guards as alerts, not one-time checks: a weekly PII grep of public HTML (contact_email, phone, stripe_payment_id, last names, exact addresses) that must return zero; an alert on any anon INSERT that lands as published; an alert on any table surfaced publicly that is near

   Build now: Ship the /admin/today Headline Tripwire tile: Weekly Player-Games Confirmed (>0 and rising; Ship the Liquidity-Per-Active-City alert (rank 1): for every city with traffic or a listed; Ship the Freshness Monitor (rank 3): last-confirmed date and Verified-vs-Public status on ; Ship the Organic-Ratio gauge (rank 5) using the existing referred_by attribution plus the ; Wire the Reach guardrail (rank 7) the moment Mailchimp keys land in Vercel: on-time-send s
   Watch: HEADLINE: Weekly Player-Games Confirmed (real games that act; Confirmation rate: played confirmations / filled tables. Gua; Coverage-to-Played ratio: (events+teachers+venues listed) / ; Cold-start liquidity per active city: seekers-this-week vs f; Funnel-leak ratios: Formed -> Filled -> Played -> Recurring,

## Love Map for Find My Mahj Game: the ranked, build-ready moments and features that turn a one-time user into someone who 
_Love is "I found my people, and a real human had my back the whole way" — so close the silent dead-ends at the hopeful moments, turn the table-fills email into a belonging ceremony, and make the standing weekly game the thing she would grieve to lose._

1. **The Welcome Back: a warm, instant reply to every 'I Want to Play' and Bench signup** (Low (1 email template + wire into 2 existing handlers; suppression/logging already flow through lib/email.ts), Every single new player feels personally received from the f)
   - Build: In app/api/want-to-play/route.ts (and the Bench join), send the player a real acknowledgment email/SMS the instant they sign up, not just the internal hello@ alert that exists today. Founder-voice, signed by Shauna: 'I got your note. I am personally looking for your people near [city]. Here is what 
2. **The Table-Fills moment as a belonging ceremony (not a logistics email)** (Low (copy rewrite in 1 route + 1 page; no schema, no new infra), Raises first-game played-confirmation rate (the North Star g)
   - Build: Rewrite the 'Your mahjong table is full!' email in app/api/tables/claim/route.ts to feel like a celebration of a group being born, not a coordination memo. Keep the safety block and reply-to-host logic exactly as-is. Add: a warm founder line ('A new table just came to life in [city] — congratulation
3. **The Standing Game ('Run It Back' → real recurrence) loop she would grieve to lose** (Medium (clone-with-roster + is_recurring + root_id migration; A/B harness on first tables), Drives the depth metric (recurring tables active this week) )
   - Build: Promote the existing minimal Run It Back link (already IN CODE on the played=yes page, app/played/page.tsx) into the full clone-with-roster + is_recurring flow that RULINGS.md scopes as BUILD. After a 'yes, we played' confirmation, one tap re-creates the SAME group for next week, keeps the lineage (
4. **The 'You found your people' recognition at the third played game** (Medium (depends on game_sessions + tables.root_id; one templated email + a flag), Creates the story players tell their friends ('this little s)
   - Build: When the system records a group's THIRD confirmed played session with the same lineage (the conversion event already defined in RULINGS.md), fire a private, founder-voice email to those players: 'You did it. Three games in, this is your group now. I'm so glad you found each other.' No points, no bad
5. **The Bench: an approve-gated city seat pool so no one waits alone** (Medium-High (seat pool schema, daily matcher, host-approval gate, match-confirm flow already partially built in app/match/confirm), Turns scattered, lonely demand into formed tables; raises Ta)
   - Build: Ship the ONE Bench that RULINGS.md names BUILD #1: Fourth Chair + Sub Bench + Re-homing + Snowbird + Mover merged into one city seat pool with one daily, founder-approve-gated matcher ('this week vs ongoing' is a field). Host approval before a stranger joins is the non-negotiable gate (no pooling wi
6. **Safety made visible as warmth, not fine print** (Low (copy + placement; logic already exists), Higher claim-completion at the moment of hesitation, fewer d)
   - Build: Keep every existing safety guarantee (no addresses, no phone/email shared, public-place first game — all IN CODE) and surface them as a reassuring, plain-language promise at the three anxiety points: the claim-a-seat step (app/t/[code] ClaimClient), the table-fills email, and the Bench join. Frame a

   Build now: 1. The Welcome Back reply: instantly acknowledge every 'I Want to Play' and Bench signup i; 2. Rewrite the table-fills email and on-site full-state as a belonging ceremony ('you four; 3. Ship the full Run It Back loop (clone-with-roster + is_recurring + root_id) so a played; 4. Ship the Bench (RULINGS BUILD #1): one approve-gated city seat pool with daily matcher,; 5. Make safety visible as warmth at the three anxiety points (claim seat, table-fills, Ben
   Watch: North Star: Weekly Player-Games Confirmed, with the >=50% co; PTWM (Players To Whom we gave belonging): players with 2+ co; Conversion-event count: groups reaching their THIRD confirme; Welcome Back response rate: of new 'I want to play' / Bench ; Recurring tables active this week (the Depth dimension): sta

