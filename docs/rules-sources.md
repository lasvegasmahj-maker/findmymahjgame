# Rules sources and provenance

Moved 2026-09-05: the corpus and its provenance rules now live in the shared package mahj-ask-core (`docs/RULES-SOURCES.md` there). This file is kept as a pointer so old links resolve. See `docs/ask-shared-core.md`.
